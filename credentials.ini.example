; WiFi and MQTT credentials — copy this to credentials.ini and fill in
; credentials.ini is gitignored and will not be committed
[credentials]
build_flags =
    -DWIFI_SSID_VALUE='"your_wifi_ssid"'
    -DWIFI_PASS_VALUE='"your_wifi_password"'
    -DMQTT_SERVER_VALUE='"192.168.1.xxx"'
